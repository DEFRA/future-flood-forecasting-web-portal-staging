<?xml version="1.0" encoding="UTF-8"?> 
<databaseChangeLog 
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog" 
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd"> 
  <changeSet author="awallace" id="changelog-create-forecast-location-datum-column.xml">
  <sql>
    IF COL_LENGTH('${fffsWebPortalStagingDbStagingSchema}.FORECAST_LOCATION','DATUM_ID') IS NULL 
        ALTER TABLE ${fffsWebPortalStagingDbStagingSchema}.FORECAST_LOCATION
          ADD DATUM_ID UNIQUEIDENTIFIER NOT NULL
          CONSTRAINT FK_DATUM
          FOREIGN KEY (DATUM_ID)
          REFERENCES ${fffsWebPortalStagingDbStagingSchema}.DATUM_LOOKUP (ID) 
  </sql>
  <rollback>
  <sql>
    IF OBJECT_ID('${fffsWebPortalStagingDbStagingSchema}.FK_DATUM','F') IS NOT NULL 
          DROP CONSTRINT FK_DATUM

    IF COL_LENGTH('${fffsWebPortalStagingDbStagingSchema}.FORECAST_LOCATION','DATUM_ID') IS NOT NULL 
          DROP COLUMN DATUM_ID
  </sql>
  </rollback>
  </changeSet>
</databaseChangeLog>
